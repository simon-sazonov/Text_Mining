# Copy to .env and fill in your own values. .env is gitignored — never commit real tokens.
# Hugging Face token (Read scope) from https://huggingface.co/settings/tokens — optional,
# lifts anonymous rate limits on model downloads.
HF_TOKEN=hf_your_token_here
